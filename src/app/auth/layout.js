export const metadata = {
  title: "Login / Sign Up — WingAgent",
  description: "Access or create your WingAgent account securely.",
};

export default function AuthLayout({ children }) {
  return children;
}